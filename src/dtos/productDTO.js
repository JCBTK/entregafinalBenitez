class ProductDTO {
    constructor({ _id, name, price, stock }) {
        this.id = _id;
        this.name = name;
        this.price = price;
        this.stock = stock;
    }
}

export default ProductDTO;
